# URL de conexion a Neon.tech (PostgreSQL)
DATABASE_URL="postgresql://neondb_owner:<TU_PASSWORD>@<ENDPOINT>.neon.tech/neondb?sslmode=require"

# Puerto donde corre el backend (coincidir con frontend VITE_BACKEND_API_URL)
PORT=4000

# JWT - Genera secrets con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=<TU_JWT_SECRET>
JWT_REFRESH_SECRET=<TU_JWT_REFRESH_SECRET>
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# CORS - Origen(es) permitido(s), separados por coma
# En desarrollo: dejar vacío (permite todo)
# En producción: https://tu-app.vercel.app
CORS_ORIGIN=

# Instrucciones: copia este archivo a `.env` y reemplaza los placeholders
# Luego desde la carpeta `backend` ejecuta `npm run dev` para reiniciar el servidor.
